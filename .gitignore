# Python
venv/
__pycache__/
*.pyc

# Local config (may contain API keys / cookies)
config.json
.env
.env.*
!.env.example

# Raw scrape dumps + debug artifacts (regenerated on every run)
scrape.log
fetch.log
afl_medical_room.html
grundy.html
injury_raw.html
pg_anderson.html
sc_be_raw.html
sc_season_raw.html

# Archives
*.zip

# Tweet post log (local state)
tweeted.json
careers.json

# Web Push runtime state (per-device sent ids)
notify_sent.json

# Claude quality agent runtime artifacts
agent.log
agent_report.json
proposed_upstream_fixes.json
.fetch_data_sig
.twitter_rss_state
.fixture_cache.json

# JS dev — local-only
node_modules/
_check.js
_tmp_*.py

# Scraper signature cache
.scrape_sig
